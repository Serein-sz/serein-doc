{
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "docs:deploy": "pnpm run docs:build && tsx deploy.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "vitepress": "^1.5.0"
  },
  "dependencies": {
    "picocolors": "^1.1.1"
  }
}
